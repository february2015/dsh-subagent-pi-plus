# Optional Profile Bundle: register the Pi provider on the Host plane only.
# Agent Presets grant the model-facing tool independently.
- insert:
    - id: subagent-pi-plus
      name: 'dsh-subagent-pi-plus'
